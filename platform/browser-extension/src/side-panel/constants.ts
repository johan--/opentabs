/** Duration (ms) to display toggle-error alerts before auto-dismissing */
export const ERROR_DISPLAY_DURATION_MS = 3000;
/** Duration (ms) the "click again to confirm" state stays active before reverting */
export const CONFIRMATION_DISPLAY_DURATION_MS = 3000;
/** Duration (ms) to show "copied" feedback after copying to clipboard */
export const COPIED_INDICATOR_DURATION_MS = 2000;
/** Interval (ms) between countdown bar progress updates */
export const COUNTDOWN_POLL_INTERVAL_MS = 200;
