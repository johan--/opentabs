---
title: Plugin System
description: How OpenTabs discovers, registers, and injects plugins — from npm packages to adapter IIFEs running in browser tabs.
lastUpdated: 2026-02-20
---

## Plugin Anatomy

A plugin is an npm package with three key files:

<Table>
  <Table.Header>
    <Table.Row>
      <Table.Head>File</Table.Head>
      <Table.Head>Purpose</Table.Head>
    </Table.Row>
  </Table.Header>
  <Table.Body>
    <Table.Row>
      <Table.Cell className="font-medium">`package.json`</Table.Cell>
      <Table.Cell>Plugin metadata via the `opentabs` field: `displayName`, `description`, and `urlPatterns`</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell className="font-medium">`dist/tools.json`</Table.Cell>
      <Table.Cell>Tool definitions with JSON Schema input/output schemas (generated by `opentabs build`)</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell className="font-medium">`dist/adapter.iife.js`</Table.Cell>
      <Table.Cell>Self-contained IIFE bundle that runs in the browser page's MAIN world context</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

Plugins extend the `OpenTabsPlugin` base class from `@opentabs-dev/plugin-sdk` and define tools using the `defineTool()` factory. The `opentabs build` CLI compiles the plugin source into `dist/adapter.iife.js` and `dist/tools.json`.

## Discovery

The MCP server discovers plugins from the `plugins` array in `~/.opentabs/config.json`. The array accepts both local directory paths and npm package names — a single unified list for all plugin sources.

```json
{
  "plugins": [
    "../plugins/slack",
    "/Users/me/my-plugin",
    "opentabs-plugin-github",
    "@myorg/opentabs-plugin-jira"
  ]
}
```

The server determines the type of each entry by its prefix:

### Local Plugins

Entries starting with `./`, `../`, `/`, or `~/` are treated as local directory paths.

- **Relative paths** are resolved from the config directory (`~/.opentabs/` by default)
- **Absolute paths** and `~/` paths are used directly (with `~/` expanded to `$HOME`)
- **Security**: all paths must resolve under `$HOME` or `$TMPDIR` after `realpath()` symlink resolution — paths outside allowed roots are rejected and logged

### npm Plugins

All other entries are treated as npm package names and resolved via `Bun.resolveSync` from the server's `node_modules`.

### Precedence

If the same plugin name appears from both a local path and an npm package in the array, the local copy takes precedence.

## Plugin Metadata

Plugin metadata is declared in `package.json` via the `opentabs` field:

```json
{
  "name": "opentabs-plugin-slack",
  "version": "0.0.1",
  "opentabs": {
    "displayName": "Slack",
    "description": "Give AI agents access to Slack workspaces",
    "urlPatterns": ["*://*.slack.com/*"]
  }
}
```

Tool definitions live in `dist/tools.json`, generated by `opentabs build`:

```json
[
  {
    "name": "send_message",
    "displayName": "Send Message",
    "description": "Send a message to a Slack channel or thread",
    "icon": "send",
    "input_schema": { "type": "object", "properties": { "...": {} } },
    "output_schema": { "type": "object", "properties": { "...": {} } }
  }
]
```

### Validation Rules

- **Plugin name**: lowercase alphanumeric with hyphens (`^[a-z0-9]+(-[a-z0-9]+)*$`). Reserved names are rejected: `system`, `browser`, `opentabs`, `extension`, `config`, `plugin`, `tool`, `mcp`.
- **URL patterns**: Chrome match patterns (e.g., `*://*.slack.com/*`). Overly broad patterns like `*://*/*` or `<all_urls>` are rejected. Bare TLD wildcards like `*.com` are rejected — patterns must include a specific domain (e.g., `*.example.com`). Maximum 5 path wildcards per pattern.
- **Tools**: At least one tool is required. Tool descriptions are capped at 1000 characters. Each tool has `input_schema` and `output_schema` in JSON Schema format (serialized from Zod via `z.toJSONSchema()`).
- **Adapter hash**: SHA-256 of the adapter IIFE content, set by the `opentabs build` CLI.
- **IIFE size**: Maximum 5 MB per adapter.

## Tool Prefixing

Plugin tools are exposed to MCP clients with a `<plugin>_<tool>` prefix to prevent name collisions:

<Table>
  <Table.Header>
    <Table.Row>
      <Table.Head>Plugin</Table.Head>
      <Table.Head>Tool</Table.Head>
      <Table.Head>MCP Name</Table.Head>
    </Table.Row>
  </Table.Header>
  <Table.Body>
    <Table.Row>
      <Table.Cell>`slack`</Table.Cell>
      <Table.Cell>`send_message`</Table.Cell>
      <Table.Cell className="font-medium">`slack_send_message`</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell>`slack`</Table.Cell>
      <Table.Cell>`read_messages`</Table.Cell>
      <Table.Cell className="font-medium">`slack_read_messages`</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell>`github`</Table.Cell>
      <Table.Cell>`create_issue`</Table.Cell>
      <Table.Cell className="font-medium">`github_create_issue`</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

Individual tools can be disabled via the `tools` field in `config.json`:

```json
{
  "tools": {
    "slack_send_message": false
  }
}
```

## Trust Tiers

Each plugin is assigned a trust tier based on its source. The tier is prefixed to tool descriptions in the MCP tool list so AI agents can assess trust:

<Table>
  <Table.Header>
    <Table.Row>
      <Table.Head>Tier</Table.Head>
      <Table.Head>Source</Table.Head>
      <Table.Head>MCP Description Prefix</Table.Head>
    </Table.Row>
  </Table.Header>
  <Table.Body>
    <Table.Row>
      <Table.Cell className="font-medium">Official</Table.Cell>
      <Table.Cell>`@opentabs-dev/*` npm packages</Table.Cell>
      <Table.Cell>`[Official]`</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell className="font-medium">Community</Table.Cell>
      <Table.Cell>Other npm packages</Table.Cell>
      <Table.Cell>`[Community plugin — unverified]`</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell className="font-medium">Local</Table.Cell>
      <Table.Cell>File paths from `config.plugins`</Table.Cell>
      <Table.Cell>`[Local plugin]`</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

Official plugins are published by the OpenTabs team. Community plugins are published to npm by third parties and are not verified. Local plugins run from the developer's machine.

## Adapter Injection

When a user opens a tab that matches a plugin's URL patterns, the Chrome extension injects the plugin's adapter into the page.

### How Injection Works

1. The extension detects a matching tab via `chrome.tabs.query({url: pattern})`
2. It injects the adapter IIFE using file-based injection:

```typescript
chrome.scripting.executeScript({
  target: { tabId },
  world: "MAIN",
  files: [`adapters/${pluginName}.js`],
});
```

3. The IIFE executes immediately and registers itself:

```javascript
globalThis.__openTabs = globalThis.__openTabs || { adapters: {} };
globalThis.__openTabs.adapters["slack"] = {
  version: "0.0.1",
  __adapterHash: "a1b2c3...",
  handle: async (tool, input) => {
    /* ... */
  },
  teardown: () => {
    /* ... */
  },
};
```

### Why File-Based Injection

File-based injection (`files:` array) bypasses page Content Security Policy (CSP) restrictions. Inline script injection would be blocked by strict CSP headers on sites like Slack and GitHub. Because adapter files are bundled with the extension, Chrome treats them as extension resources exempt from page CSP.

### MAIN World

Adapters run in the page's MAIN world — the same JavaScript context as the page itself. This gives adapters full access to the page's DOM, cookies, `localStorage`, `fetch` (with the page's session cookies), and any global variables.

### Hash Verification

After injection, the extension reads the adapter's `__adapterHash` property and compares it to the expected hash from the manifest. If the hashes don't match (e.g., due to a partial file write), injection is retried once after 200 ms. A persistent mismatch raises an error.

## File Watcher

The MCP server watches local plugin directories for changes to `dist/tools.json` and `dist/adapter.iife.js`. Changes are debounced at 200 ms.

When **`dist/tools.json`** changes:

1. Tool definitions are re-read and validated
2. IIFE is also re-read (builds typically update both)
3. MCP tool list is rebuilt and sent to connected MCP clients
4. Extension is notified via `plugin.update` to re-inject the adapter

When the **IIFE** changes:

1. IIFE is re-read from disk
2. Embedded adapter hash is extracted
3. Extension is notified via `plugin.update`

The file watcher also monitors paths for plugins that failed initial discovery (e.g., build artifacts not yet generated). When those files appear, the plugin is loaded and registered automatically.

### Stale Watcher Detection

A fallback mtime-based poller runs every 30 seconds to detect stale `fs.watch()` instances (a known issue on macOS with long-running processes). If file modifications are detected by polling but not by the watcher, the same change handlers are triggered as a safety net.

## Tool Dispatch

When an AI agent calls a plugin tool:

1. MCP server receives the tool call via Streamable HTTP at `/mcp`
2. Input is validated against the tool's JSON Schema
3. Server enforces a concurrency limit (max 5 concurrent dispatches per plugin)
4. Server sends a `tool.dispatch` JSON-RPC message over WebSocket to the extension
5. Extension finds the best matching tab by URL pattern and [tab ranking](/docs/concepts/tab-state-machine)
6. Extension executes the adapter's `handle()` function in the matching tab's MAIN world
7. Result flows back through WebSocket to the MCP server
8. Server validates the output and returns it to the AI agent
